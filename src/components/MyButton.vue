<script setup lang="ts">
  // const props = defineProps({
  // 	name: {
  // 		type: String,
  // 		default: 'MyButton',
  // 	},
  // })

  // const emit = defineEmits(['addToCart'])

  // // Use the addToCart function like this:
  // // Eg-1: addToCart('pizza', 10)
  // // Eg-2: (event) => addToCart('pizza', 10, event)
  // const addToCart = (item, quantity /*, event*/) => {
  // 	emit('addToCart', { item, quantity })
  // }
</script>
<template>
  <a
    href="#"
    class="bg-primary-500 hover:bg-primary-600 inline-block m-1 px-6 py-2 rounded rounded-3xl text-white"
    >Order Now</a
  >
</template>
<style scoped></style>
